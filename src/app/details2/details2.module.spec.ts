import { Details2Module } from './details2.module';

describe('Details2Module', () => {
  let details2Module: Details2Module;

  beforeEach(() => {
    details2Module = new Details2Module();
  });

  it('should create an instance', () => {
    expect(details2Module).toBeTruthy();
  });
});
